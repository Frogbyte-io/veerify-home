---
import { withLocale, type Locale, type SiteDictionary } from '../i18n/site'

interface Props {
  locale: Locale
  t: SiteDictionary['navbar']
}

const { locale, t } = Astro.props
const homeHref = withLocale(locale)
const isEnglishLocale = locale === 'en'
---

<header class="sticky top-0 z-50 border-b border-border/40 bg-background/75 backdrop-blur-xl">
  {isEnglishLocale && (
    <div
      id="locale-suggestion"
      class="hidden border-b border-border/40 bg-secondary/40"
      role="status"
      aria-live="polite"
    >
      <div class="mx-auto flex max-w-7xl items-center justify-between gap-3 px-6 py-2.5 text-xs text-muted-foreground lg:px-8">
        <p>Did you know we are a Norwegian company? Check out our local site!</p>
        <div class="flex items-center gap-2">
          <a
            id="locale-suggestion-link"
            href="/no"
            class="rounded-md border border-border/60 px-2.5 py-1 font-medium text-foreground transition-colors hover:bg-background"
          >
            Visit Norwegian site
          </a>
          <button
            id="locale-suggestion-dismiss"
            type="button"
            class="rounded-md px-2 py-1 transition-colors hover:text-foreground"
            aria-label="Dismiss local site suggestion"
          >
            Dismiss
          </button>
        </div>
      </div>
    </div>
  )}

  <div class="mx-auto flex h-16 max-w-7xl items-center justify-between px-6 lg:px-8">
    <a href={homeHref} class="group flex items-center gap-2.5">
      <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg border border-border bg-secondary transition-transform group-hover:scale-105">
        <img src="/veerify.svg" alt="Veerify" class="h-5 w-5" />
      </div>
      <span class="font-display text-[17px] font-semibold tracking-tight">Veerify</span>
    </a>

    <nav class="hidden items-center gap-7 md:flex" aria-label="Main navigation">
      <a href={`${homeHref}#features`} class="text-sm text-muted-foreground transition-colors hover:text-foreground">{t.features}</a>
      <a href={`${homeHref}#how-it-works`} class="text-sm text-muted-foreground transition-colors hover:text-foreground">{t.howItWorks}</a>
      <a href={`${homeHref}#pricing`} class="text-sm text-muted-foreground transition-colors hover:text-foreground">{t.pricing}</a>
      <a href="https://feedback.veerify.io" class="text-sm text-muted-foreground transition-colors hover:text-foreground">{t.feedback}</a>
    </nav>

    <div class="flex items-center gap-3">
      {locale !== 'en' && (
        <a
          href="/"
          class="hidden text-sm text-muted-foreground transition-colors hover:text-foreground sm:block"
          aria-label="Switch language to English"
        >
          English
        </a>
      )}
<button
        id="dark-toggle"
        class="flex h-9 w-9 items-center justify-center rounded-lg border border-border text-muted-foreground transition-colors hover:border-border/60 hover:text-foreground"
        aria-label={t.darkModeToggle}
      >
        <svg class="icon-sun" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="4"/>
          <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"/>
        </svg>
        <svg class="icon-moon hidden" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
      </button>

      <a
        href="https://github.com/Frogbyte-io/veerify"
        target="_blank"
        rel="noopener noreferrer"
        class="hidden h-9 w-9 items-center justify-center rounded-lg border border-border text-muted-foreground transition-colors hover:border-border/60 hover:text-foreground sm:flex"
        aria-label={t.openSourceTitle}
        title={t.openSourceTitle}
      >
        <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
          <path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0 1 12 6.844a9.59 9.59 0 0 1 2.504.337c1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.02 10.02 0 0 0 22 12.017C22 6.484 17.522 2 12 2z"/>
        </svg>
      </a>

      <a
        href="https://app.veerify.io/login"
        class="hidden text-sm text-muted-foreground transition-colors hover:text-foreground sm:block"
      >
        {t.signIn}
      </a>
      <a
        href="https://app.veerify.io/get-started"
        class="inline-flex h-9 items-center justify-center rounded-lg bg-primary px-4 text-sm font-medium text-primary-foreground shadow-md shadow-primary/20 transition-all hover:opacity-90"
      >
        {t.getStartedFree}
      </a>
    </div>
  </div>
</header>

<script>
  const btn = document.getElementById('dark-toggle')
  const html = document.documentElement
  const sunIcon = document.querySelector('.icon-sun') as HTMLElement | null
  const moonIcon = document.querySelector('.icon-moon') as HTMLElement | null
  const localeSuggestion = document.getElementById('locale-suggestion') as HTMLElement | null
  const localeSuggestionLink = document.getElementById('locale-suggestion-link') as HTMLAnchorElement | null
  const localeSuggestionDismiss = document.getElementById('locale-suggestion-dismiss') as HTMLButtonElement | null

  if (btn && sunIcon && moonIcon) {
    function updateIcons() {
      const isDark = html.classList.contains('dark')
      sunIcon.classList.toggle('hidden', !isDark)
      moonIcon.classList.toggle('hidden', isDark)
    }

    updateIcons()

    btn.addEventListener('click', () => {
      const isDark = html.classList.toggle('dark')
      localStorage.setItem('theme', isDark ? 'dark' : 'light')
      updateIcons()
    })
  }

  if (localeSuggestion && localeSuggestionLink && localeSuggestionDismiss) {
    const dismissedKey = 'veerify-locale-suggestion-dismissed-v1'

    function isLikelyNorwegianVisitor() {
      const languages = navigator.languages?.length ? navigator.languages : [navigator.language]
      const hasNorwegianLanguage = languages.some(language => /^(no|nb|nn)(-|$)/i.test(language || ''))
      const timeZone = Intl.DateTimeFormat().resolvedOptions().timeZone || ''
      const isNorwayTimeZone = timeZone.toLowerCase() === 'europe/oslo'

      return hasNorwegianLanguage || isNorwayTimeZone
    }

    function buildNorwegianPath(pathname: string) {
      if (pathname === '/') return '/no'
      if (pathname === '/en') return '/no'
      if (pathname.startsWith('/en/')) return `/no${pathname.slice(3)}`
      return `/no${pathname}`
    }

    const isDismissed = localStorage.getItem(dismissedKey) === '1'
    const isAlreadyOnNorwegianPath = window.location.pathname === '/no' || window.location.pathname.startsWith('/no/')

    if (!isDismissed && !isAlreadyOnNorwegianPath && isLikelyNorwegianVisitor()) {
      const targetPath = buildNorwegianPath(window.location.pathname)
      localeSuggestionLink.href = `${targetPath}${window.location.search}${window.location.hash}`
      localeSuggestion.classList.remove('hidden')
    }

    localeSuggestionDismiss.addEventListener('click', () => {
      localStorage.setItem(dismissedKey, '1')
      localeSuggestion.classList.add('hidden')
    })

    localeSuggestionLink.addEventListener('click', () => {
      localStorage.setItem(dismissedKey, '1')
    })
  }
</script>
